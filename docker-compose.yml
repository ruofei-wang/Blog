version: '3.7'
services:
  blog:
    image: kkrepo/blog:v1.0
    ports:
      - "8080:8080"
    volumes:
      - /opt/blog/data:/opt/blog:ro
    depends_on:
      - redis
    links:
      - redis:redis

  redis:
    image: redis:latest
    restart: always
    ports:
      - "6379:6379"
